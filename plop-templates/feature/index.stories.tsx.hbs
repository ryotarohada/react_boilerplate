import type { Meta, StoryObj } from "@storybook/react";
import { {{pascalCase name}}Feature } from "./index";

const meta = {
  component: {{pascalCase name}}Feature,
  parameters: {},
} satisfies Meta<typeof {{pascalCase name}}Feature>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Base: Story = {
  args: {},
};
